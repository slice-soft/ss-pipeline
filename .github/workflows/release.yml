name: Release Pipeline

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-pipeline:
    uses: ./.github/workflows/create-release.yml
    secrets: inherit