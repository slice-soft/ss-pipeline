name: Validate PR - Conventional Commits

# Reusable workflow that validates PR titles against Conventional Commits.
# Requires the title format: type(scope): description
# Allowed types: feat, fix, chore, docs, refactor, perf, test, ci, build

on:
  workflow_call:
jobs:
  validate-title:
    runs-on: ubuntu-latest
    steps:
      - name: Validate PR title (conventional commits)
        uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            chore
            docs
            refactor
            perf
            test
            ci
            build
          requireScope: false
